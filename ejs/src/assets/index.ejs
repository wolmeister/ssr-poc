<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>SSR POC - EJS</title>

    <style>
      body {
        margin: 0;
      }

      .container {
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        flex-direction: column;
      }

      .avatar {
        width: 150px;
        height: 150px;
        border-radius: 50%;
        margin-bottom: 8px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <img src="<%= user.avatarUrl %>" alt="user-avatar" class="avatar" />
      <span><%= user.name %></span>
    </div>
  </body>
</html>
